'use strict';

/**
 * user-google-token controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::user-google-token.user-google-token');
